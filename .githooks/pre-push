#!/bin/sh
# 推送前门禁：完整 test:gate（eslint + diff --check + 全量 vitest + mobile-remote）。
# 安装：git config core.hooksPath .githooks
set -e
cd "$(git rev-parse --show-toplevel)"

echo "[pre-push] npm run test:gate..."
npm run test:gate

echo "[pre-push] OK"
